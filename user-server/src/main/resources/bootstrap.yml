eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
server:
  port: 8009
spring:
  application:
    name: user-server
  cloud:
    config:
      uri: http://192.168.99.1:8008/        #Config server的uri
      name: user    # 对应 {application} 部分
      profile: prod # 对应 {profile} 部分
      label: master  #指定分支
